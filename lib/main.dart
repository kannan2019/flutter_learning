import 'package:flutter/material.dart';

//* void main is the starting point of the flutter app
void main() => runApp(
      MaterialApp(
        home: Center(
          child: Text('Kannan'),
        ),
      ),
    );
